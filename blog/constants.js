const baseUrl = {
    client: "http://localhost:3001",
    server: "http://localhost:3080"
};

const maxNumOfClapsPerUserPerPost = 5;
const maxTitleLength = 100;

module.exports = { baseUrl, maxNumOfClapsPerUserPerPost, maxTitleLength };